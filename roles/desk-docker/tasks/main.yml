- name: install docker
  package:
    name:
      - docker
      - docker-compose
    state: present

- name: "Adding user {{ lookup('users', WORKSTATION_USER).username }} to relevant docker usergroup"
  user: 
    name: "{{ lookup('users', WORKSTATION_USER).username }}"
    groups: docker
    append: yes
