---
sso:
  bond: 1
  enabled: "{{ 'web-app-keycloak' in group_names }}"
  shared: "{{ 'web-app-keycloak' in group_names }}"
  flavor: oidc
asset:
  bond: 1
  enabled: "{{ 'web-svc-asset' in group_names }}"
  shared: "{{ 'web-svc-asset' in group_names }}"
# nocheck: dynamic-flag — dashboard renders the CDN base URL into javascript.js (DASHBOARD_JS_BASE_URL) unconditionally; runtime JS load has no fallback, so this is a hard dependency.
cdn:
  bond: 1
  enabled: true
  shared: true
simpleicons:
  bond: 1
  enabled: "{{ 'web-svc-simpleicons' in group_names }}"
  shared: "{{ 'web-svc-simpleicons' in group_names }}"
logout:
  bond: 1
  enabled: "{{ 'web-svc-logout' in group_names }}"
  shared: "{{ 'web-svc-logout' in group_names }}"
matomo:
  bond: 1
  enabled: "{{ 'web-app-matomo' in group_names }}"
  shared: "{{ 'web-app-matomo' in group_names }}"
# nocheck: playwright-service-flag — self-provider
dashboard:
  healthcheck:
    flavor: tcp
  modes:
    compose:
      enabled: true
    swarm:
      enabled: true
  bond: 1
  enabled: true
  shared: true
  name: dashboard
  image: ghcr.io/kevinveenbirkenbach/port-ui
  version: 2.2.2
  min_storage: 549MB
  cpus: "0.3"
  mem_reservation: 256m
  mem_limit: 512m
  pids_limit: 256
  ports:
    local:
      http: 8029
    internal:
      http: 5000
  proxy:
    service_key: dashboard
  run_after:
    - web-app-matomo
  lifecycle: beta
css:
  bond: 1
  enabled: "{{ 'web-svc-css' in group_names }}"
  shared: "{{ 'web-svc-css' in group_names }}"
# nocheck: dynamic-flag
# nocheck: playwright-service-flag — canonical:cdn alias of static-asset provider
javascript:
  bond: 1
  enabled: true
  shared: true
prometheus:
  bond: 1
  enabled: "{{ 'web-app-prometheus' in group_names }}"
  shared: "{{ 'web-app-prometheus' in group_names }}"

# nocheck: playwright-service-flag
# nocheck: variants-coverage — sso-variant-tor requires the onion on in every variant, so the false polarity has no variant left to live in
tor:
  bond: 0.5
  enabled: "{{ 'svc-net-tor' in group_names }}"
  shared: true # nocheck: dynamic-flag
