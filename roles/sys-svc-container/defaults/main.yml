# ------------------------------------------------------------
# Docker-in-Docker DNS test images
# Auto-mirrored via images discovery; consumers resolve through
# lookup('image', <service>, 'ref').
# ------------------------------------------------------------
images:
  busybox:
    image: busybox
    version: "1.36"
  node:
    image: node
    version: "20-alpine"
