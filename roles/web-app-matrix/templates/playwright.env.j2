APP_BASE_URL={{ lookup('tls', 'web-app-dashboard', 'url.base') | dotenv_quote }}
OIDC_ISSUER_URL={{ OIDC.CLIENT.ISSUER_URL | dotenv_quote }}
ELEMENT_BASE_URL={{ ('https://' ~ (lookup('applications', application_id).server.domains.canonical.element)) | dotenv_quote }}
MATRIX_BASE_URL={{ ('https://' ~ (lookup('applications', application_id).server.domains.canonical.synapse)) | dotenv_quote }}
MATRIX_SERVER_NAME={{ MATRIX_SERVER_NAME | dotenv_quote }}
ADMIN_USERNAME={{ lookup('users', 'administrator').username | dotenv_quote }}
ADMIN_PASSWORD={{ lookup('users', 'administrator').password | dotenv_quote }}
BIBER_USERNAME={{ lookup('users', 'biber').username | dotenv_quote }}
BIBER_PASSWORD={{ lookup('users', 'biber').password | dotenv_quote }}
CANONICAL_DOMAIN={{ (lookup('applications', application_id).server.domains.canonical.element) | dotenv_quote }}
