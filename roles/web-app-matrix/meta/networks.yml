---
local:
  subnet: 192.168.32.0/24
  force_bridge: "{{ lookup('config', 'web-app-matrix', 'services.matrix.flavor') == 'ansible' }}"
reachability:
  single_mode: true # nocheck: network-reachability  Synapse derives server_name and every MXID from one domain
