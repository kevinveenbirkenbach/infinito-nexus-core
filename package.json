{
  "name": "infinito-nexus-js-lint",
  "version": "0.0.0",
  "private": true,
  "description": "JavaScript lint dependencies for infinito-nexus (Playwright specs + persona helpers).",
  "scripts": {
    "lint": "eslint",
    "lint:fix": "eslint --fix"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.63.0",
    "eslint": "^10.9.1",
    "eslint-plugin-playwright": "^2.10.5",
    "globals": "^17.9.0",
    "stylelint": "^17.14.1"
  },
  "overrides": {
    "brace-expansion": "^5.0.9"
  }
}
